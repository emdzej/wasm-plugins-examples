{
  "name": "@swp/plugin-pixelate",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "asc assembly/index.ts --config asconfig.json --target release",
    "clean": "rm -rf build"
  },
  "devDependencies": {
    "assemblyscript": "^0.27.29"
  }
}
